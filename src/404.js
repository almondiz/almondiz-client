import React from "react";


const NotFound = () => {
  return (
    <div className="page-wrap" style={{ display: "flex", flexDirection: "column", height: "100vh", justifyContent: "center", alignItems: "center" }}>
      <p style={{ fontSize: "2rem" }}>{`(╯°□°）╯︵ ┻━┻`}</p>
      <p style={{ marginTop: "2rem", fontSize: "1.25rem" }}>{`이게 머선 페이지고?`}</p>
    </div>
  );
};

export default NotFound;